[package]
name = "renderer"
version = "0.1.0"
edition = "2024"

[dependencies]
fastrand = "2.4.1"
image = { version = "0.25.10", default-features = false, features = ["png"] }
rayon = "1.12.0"
tobj = { version = "4.0.3", features = ["use_f64"] }

[profile.release]
lto = "fat"
